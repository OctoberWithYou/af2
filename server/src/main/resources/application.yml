server:
  port: 8080
  ws-port: 8888
  ssl-enabled: false
  # key-store-path: classpath:keystore.p12
  # key-store-password: password
  heartbeat-interval: 30
  max-idle-time: 120
  auth-username: admin
  auth-password: Sys_ljc_123

spring:
  application:
    name: ai-forward-server
  # 如果需要使用数据库，可以在这里配置
  # datasource:
  #   url: jdbc:h2:mem:testdb
  #   driver-class-name: org.h2.Driver

logging:
  level:
    root: INFO
    org.ljc.server: DEBUG
    io.netty: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"